# Root-level node_modules (if monorepo)
node_modules/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS junk
.DS_Store
Thumbs.db

# Env files
.env
.env.*   # For .env.local, .env.production, etc.

# VSCode
.vscode/

# Dependency locks (if you're only using one lock type)
package-lock.json
yarn.lock
pnpm-lock.yaml

# Build output
dist/
build/

# Optional: If you're using Docker
docker-compose.override.yml
*.local